# Настройка окружений — AI Personal Chef

## 📌 Цель
Документ описывает конфигурацию окружений (Dev, Staging, Prod), управление переменными окружения, стратегию ротации секретов и планы резервного копирования/восстановления.

---

## ⚙️ Окружения

### Dev
- Локальный запуск: `uvicorn main:app --reload --port 8000`
- Используется SQLite как БД
- Ollama и RabbitMQ запускаются локально
- Nginx работает как reverse proxy
- Логи пишутся в консоль + LangFuse для отладки

### Staging
- Развёртывание на тестовом сервере
- RabbitMQ и Ollama запускаются в контейнерах
- БД: SQLite или PostgreSQL (тестовый инстанс)
- Nginx с базовой конфигурацией TLS
- Используется для интеграционного тестирования

### Prod
- Развёртывание на выделенном сервере/кластер Kubernetes
- RabbitMQ и Ollama в отдельных контейнерах
- БД: PostgreSQL (боевой инстанс)
- Nginx с полной конфигурацией TLS и балансировкой
- Логи и метрики через LangFuse
- Резервное копирование включено

---

## 🔑 Environment Variables

Пример `.env` файла:

- **Dev**: тестовые ключи, локальные сервисы  
- **Staging**: отдельные ключи, ограниченные права  
- **Prod**: боевые ключи, доступ только через секрет‑менеджер  

---

## 🔒 Secrets Rotation

- Все ключи (LLM API, LangFuse, RabbitMQ) хранятся в секрет‑менеджере (например, Vault или Kubernetes Secrets).  
- Ротация каждые 90 дней.  
- Автоматическая проверка устаревших ключей.  
- Доступ к секретам только у CI/CD и администраторов.  

---

## 💾 Backup и Recovery

### Backup
- База данных (SQLite/PostgreSQL) — ежедневный дамп.  
- RabbitMQ — экспорт состояния очередей раз в сутки.  
- Конфигурация Nginx и `.env` — хранение в Git + зашифрованные копии.  

### Recovery
- Восстановление БД из последнего дампа.  
- Перезапуск контейнеров Ollama и RabbitMQ.  
- Проверка доступности API и фронтенда.  
- Smoke‑тест: регистрация пользователя, загрузка фото, генерация рецепта.  

---

## 📂 Итог
Документ фиксирует:
- Dev/Staging/Prod окружения  
- Управление переменными окружения  
- Стратегию ротации секретов  
- Планы резервного копирования и восстановления  

**Результат:** `docs/environments.md` — готовая инструкция для настройки окружений.

